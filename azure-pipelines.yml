trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  buildConfiguration: 'Release'
  testPath: '$(Build.SourcesDirectory)/CPX.Configuration.Test'
  coveragePath: '$(testPath)/TestResults/Coverage'
  coveragePathFile: '$(coveragePath)/coverage.cobertura.xml'

steps:
- task: SonarCloudPrepare@1
  inputs:
    SonarCloud: 'b595c2a5-7f6b-4a6d-a5a9-d7d2422d323c'
    organization: 'cassianopeixoto-github'
    projectKey: 'cassianopeixoto'
    projectName: 'cpx.configuration'
    extraProperties: 'sonar.cs.opencover.reportsPaths=$(coveragePathFile)/coverage.opencover.xml'

- task: DotNetCoreCLI@2
  displayName: Build
  inputs:
    command: build
    projects: '**/*.csproj'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: Test
  inputs:
    command: test
    projects: '**/*Test/*.csproj'
    arguments: '--configuration $(buildConfiguration) /p:CollectCoverage=true /p:CoverletOutputFormat=“cobertura%2copencover" /p:CoverletOutput=$(coveragePathFile) /p:Exclude="[xunit*]*"'

- task: PublishCodeCoverageResults@1
  inputs:
    codeCoverageTool: 'Cobertura'
    summaryFileLocation: '$(coveragePathFile)'
    failIfCoverageEmpty: true   

- task: SonarQubeAnalyze@4
  displayName: 'SonarQubeAnalyze'    

- task: SonarCloudPublish@1
  displayName: 'SonarQubePublish'